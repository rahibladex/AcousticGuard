name: Android CI/CD

on:
  push:
    tags:
      - 'v*'

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'
          cache: gradle

      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v3
        with:
          gradle-version: 8.7

      - name: Build with Gradle
        run: gradle :app:assembleDebug --stacktrace

      - name: Prepare APK for Upload
        run: |
          mkdir -p release_assets
          cp app/build/outputs/apk/debug/*.apk release_assets/acousticguard.apk

      - name: Upload APK to Release
        uses: softprops/action-gh-release@v2
        with:
          files: release_assets/acousticguard.apk
          tag_name: ${{ github.ref_name }}
          name: Release ${{ github.ref_name }}
          body: |
            ### AcousticGuard v2.0.2
            - **Stop Alarm Option**: Silence the alarm easily.
            - **Improved UI**: Better visual feedback.
            - **Stability Fixes**: Optimized build process.
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
